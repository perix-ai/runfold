# Schemas

Machine-readable representations of the Runtime Data records defined in
`spec/`. Behavioral semantics remain in the prose specifications and RFCs;
schemas validate shape, required fields and basic value constraints.

Schemas are grouped by contract version. Published versions are immutable.
Incompatible changes require a new version directory rather than rewriting an
existing released contract.
