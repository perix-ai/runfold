# Schemas

Machine-readable, language-neutral wire schemas for the runtime-data
facilities. Behavioral semantics stay in `docs/<facility>/specification.md`; schemas
validate shape, required fields, and basic value constraints.

Schemas are grouped by facility and contract version (`event/v0/`). Published
versions are immutable; incompatible changes require a new version directory.
