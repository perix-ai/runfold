# Event schemas

Versioned language-neutral wire schemas for Session headers and Event envelopes.
They define the shared storage boundary; language-specific classes and naming
conventions do not change these JSON field names.
