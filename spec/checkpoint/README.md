# Checkpoint

Durable logical recovery boundaries for resume, restore and fork.

A checkpoint must reference an exact logical version and may optionally reference an execution-plane checkpoint without owning it.
