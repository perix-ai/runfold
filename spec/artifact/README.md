# Artifact

Versioned metadata and lineage for generated files and data products.

Artifact bytes may live in external object storage; Runtime Data owns identity, metadata, version references and lineage semantics.
