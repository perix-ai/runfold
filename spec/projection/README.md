# Projection

Derived views of canonical runtime data.

Examples include OpenTelemetry mappings, analytics schemas and query-oriented materializations. Projections are never the authoritative recovery source.
