# Conformance

Shared behavioral cases and wire fixtures that every implementation of a
facility must pass, grouped by facility and contract version (`event/v0/`).

Implementations read the exact fixture bytes and must produce the same
normalized logical result. Implementation-specific unit and integration tests
stay beside each implementation rather than here.
