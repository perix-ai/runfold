# 任务：Nexent Demo 自然人声与清晰点击动作

> 对应清单：[`../tasks.md`](../tasks.md) R41。
> 执行者：Codex。状态：已完成（2026-09-02）。

## 背景

R40 已提供普通话旁白和两个真实 Fork 入口，但系统语音仍有明显机器感；鼠标在
目标附近移动和点击的节奏过慢，点击瞬间缺少足够清楚的视觉反馈。用户要求在不
改变 Nexent、DSH 或 Event 行为的前提下重制媒体。

## 实施范围

- 将 macOS 传统系统语音替换为自然度更高的普通话神经网络语音；不向仓库写入
  凭据、缓存或生成工具依赖。
- 精简旁白和总时长，使说明更接近真人演示节奏。
- 鼠标在关键操作前快速移动到目标，短暂停留后点击；点击使用高对比度按下态、
  中心闪光和双脉冲圆环，不能仅靠缓慢放大的单个圆圈表达。
- 明确展示聊天消息 Fork 图标、轨迹分叉点选择、轨迹 Fork 按钮和刷新按钮四个
  操作；选择值与操作结果仍来自 R40 已实际验收的浏览器状态。
- 保持 `docs/event/demos/nexent/trajectory-restore-fork-demo.mp4` 路径不变，并同步
  更新封面、章节、媒体参数、哈希和制作边界说明。
- 不修改 Nexent 本地实验分支、DSH `EventTrajectory` 或 Event 实现。

## 验收

1. 普通话音轨采用神经网络人声，语速自然、清楚，无削波和长时间空白。
2. 四个关键点击在正常播放速度下一眼可辨：目标高亮、鼠标抵达、按钮按下、双
   脉冲反馈和结果切换均有独立阶段。
3. 聊天快捷 Fork 和轨迹精确 Fork 的目标、边界与结果没有改变。
4. MP4 完整解码，视频、音频参数和关键帧检查通过。
5. README、任务清单和媒体哈希同步后才可标记完成。

## 完成记录

- 旁白更换为普通话 `zh-CN-XiaoxiaoNeural` 神经语音，采用偏自然的语速与音高；
  精简脚本后总时长由 `00:01:40.63` 缩短为 `00:01:23.07`。生成时临时使用
  仓库外的 `edge-tts 7.2.8`，仓库不增加生成依赖、缓存或凭据。
- 轨迹详情、刷新、聊天 Fork、分叉点选择和轨迹 Fork 均改为快速移入、短暂停留、
  明确按下态、中心闪光、橙蓝双脉冲与同步点击音；小尺寸目标另加局部放大。
- 重新核对关键帧 `00:23.4`、`00:39.2`、`00:49.0`、`01:00.7`、`01:04.5`
  和 `01:07.0`，确认目标、选择状态、点击与结果切换可辨；旁白中的“点击”与画面
  动作已对齐。
- MP4 完整解码通过：H.264 High / yuv420p、1440×900、24 fps；AAC-LC、48 kHz、
  单声道、约 118 kb/s；平均音量 `-16.4 dB`、峰值 `-1.4 dB`，没有持续两秒
  以上的静音。
- 最终视频 SHA-256 为
  `d27fc35a58fcddade5be724a5ff971fc7482d119467fcec9a9a8377bee08f64c`；封面为
  `8d239e43655ca375ecc302e74fa2fd10ef97cfcfc9080a7e5046428f7e902f91`。
- 本次只重制 `docs/event/demos/nexent/` 媒体与说明；Nexent、DSH
  `EventTrajectory` 和 Event 实现均未修改。
